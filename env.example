# Public environment variables (can be exposed to the client)
PUBLIC_APP_NAME="RetireRight"
PUBLIC_ANALYTICS_ENABLED=false

# Optional: Analytics configuration (Umami)
# UMAMI_WEBSITE_ID=""
# UMAMI_URL=""

# Optional: Database configuration (if needed for future features)
# DATABASE_URL=""

# Optional: Email configuration (if needed for contact forms)
# SMTP_HOST=""
# SMTP_PORT=""
# SMTP_USER=""
# SMTP_PASS=""
